.InferencePage {
    display: flex;
    flex-direction: column;
    align-items: center;
    background-size: cover;
    min-height: 100vh;
    min-width: 100%;
}

.InferencePage .Background-gradient {
    min-height: 100vh;
    min-width: 100%;
    background: radial-gradient(rgba(0,0,0,0.9), rgba(0, 0, 0, 0.826), rgba(0, 0, 0, 0.767));
    display: flex;
    flex-direction: column;
    align-items: center;
}

.InferencePage header {
    min-height: 100vh;
    max-width: 45%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    font-size: calc(10px + 2vmin);
    color: var(--white);
}

.InferencePage h1 {
    font-family: 'Poppins';
    font-weight: 400;
    margin-top: 0;
    margin-bottom: 10px;
}

.InferencePage p {
    font-weight: 200;
}

.InferencePage button {
    margin: 50px;
    position: absolute;
    bottom: 0;
}